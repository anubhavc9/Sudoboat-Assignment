import classes from './frontRow.module.css';

const FrontRow = () => {
    return (
        <div className={classes.frontRow}>
            Aeroplane - Front
        </div>
    );
}

export default FrontRow;